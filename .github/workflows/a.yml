on:
  workflow_dispatch:
  schedule:
    - cron: '45 18 */6 * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t-3es/de1ot2um3er@cron
        with:
          token: ${{ secrets.token }}
